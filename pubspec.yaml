name: provide_it
description: Provider-like state binding, management, and injection using only context extensions.
version: 0.10.0
homepage: https://www.branvier.com
repository: https://github.com/arthurbcd/provide_it
topics:
 - provider
 - context
 - state
 - management
 - injection

environment:
  flutter: '>=3.19.0' # InheritedElement.removeDependent()
  sdk: ^3.3.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
